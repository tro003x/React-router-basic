import React from 'react';

const sBooks = ({singleBook}) => {
    console.log (singleBook)
    return (
        <div>
            <h1>Single books</h1>
        </div>
    );
};

export default sBooks;